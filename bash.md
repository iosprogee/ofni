#### find 
> ```
> # rm all .DS_Store
> $ find . | grep ".DS_Store"| xargs rm
>
> # count files
> $ find . -type f | wc -l
> ```

#### grep 
> ```
> # list files containing what
> $ grep -rnwi * -e what
> -whole word, -ignorecase -recursive -nth line
> 
> ```

#### temp 
> ```
> 
> ```